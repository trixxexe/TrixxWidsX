Create a complete, fully working Android app called TrixxWids which is a custom widget maker for Android home screens and lock screens inspired by iOS widget aesthetics, and this must be a production-ready app and not a prototype, and I have zero coding experience so every step and instruction must be extremely clear. The language to use is Kotlin with a minimum SDK of 26 targeting Android 8.0 and a target SDK of 34, and the build system must be Gradle with Kotlin DSL using build.gradle.kts files, and you must only use free stable well-maintained libraries from Maven Central or Google with no external paid APIs, and every single file must be 100% complete with no placeholders, no TODO comments, and no gaps or ellipsis, and the app must compile and run without any modifications after extraction. The complete project must follow this exact folder structure: a root TrixxWids folder containing an app folder which contains src/main with AndroidManifest.xml and a java/com/trixxwids/app/ folder with all Kotlin source files and a res folder containing layout with all XML layouts and drawable and values with colors.xml and strings.xml and themes.xml and xml with widget provider XMLs, and the app folder also contains build.gradle.kts and proguard-rules.pro, and the root also contains build.gradle.kts and settings.gradle.kts and gradle.properties and gradlew and gradlew.bat and a gradle/wrapper folder with gradle-wrapper.jar and gradle-wrapper.properties and a .github/workflows/build.yml file. The app must have a canvas-based widget editor as the main screen where users can visually build their widget and add elements including Text, Clock, Date, Weather placeholder, Shape with rectangle and circle options, and Image, and each element must be draggable and resizable on the canvas, and there must be a properties panel allowing the user to change font size, font color, background color, opacity, border radius, and border color, and there must be a live preview of the widget as it will appear on the home screen. The app must support a 3D tilt and gyroscope effect which when enabled makes the widget preview react to device tilt using the accelerometer and gyroscope to create a parallax depth illusion where layers shift slightly on tilt, and this must be toggleable and off by default and must gracefully disable itself if the sensor is unavailable, and this must use Android SensorManager only with no third-party library for this feature. The app must support standard Android widget sizes of 2x1, 2x2, 4x1, 4x2, and 4x4 where the user selects the size before editing and the canvas snaps to the correct aspect ratio for that size. Widgets must be saved locally using Room database storing the name, config as JSON, and thumbnail as a Bitmap saved to internal storage, and there must be a saved widgets gallery screen showing thumbnails in a grid where the user can edit, duplicate, or delete any widget, and there must be an Apply to Home Screen button that triggers Android's standard AppWidgetManager pinning flow for API 26 and above, and for lock screen there must be guidance text inside the app explaining how to set it as a live wallpaper or notification widget since Android restricts direct lock screen widget placement after API 17. The inside-app UI must have a bottom navigation bar with three tabs which are Editor, My Widgets, and Help, and must use Material 3 design throughout, and must support dark mode, and must have smooth transitions between screens with shared element transitions where relevant, and must show an onboarding flow of three simple illustrated slides only on first launch. The Help tab must contain step-by-step instructions explaining how to create a widget, how to add it to the home screen, how to enable the gyro effect, and a FAQ section. The GitHub Actions build file at .github/workflows/build.yml must trigger on every push to the main branch and use ubuntu-latest and set up JDK 17 and run ./gradlew assembleDebug and upload the resulting APK as a downloadable artifact named TrixxWids-debug.apk, and no signing is required since a debug build is fine, and the yml must use current non-deprecated GitHub Actions syntax using actions/upload-artifact@v4 and actions/setup-java@v4 and similar current versions. The libraries to use are AndroidX Core KTX, AndroidX AppCompat, Material Components 3, ConstraintLayout, Room for local database, Gson for JSON serialization of widget configs, Glide for image loading, AndroidX Navigation Component for bottom nav, and Lifecycle ViewModel with LiveData, and you must use XML layouts only and not Jetpack Compose since XML is more stable for GitHub Actions builds without a local environment. The following rules must never be violated: every Kotlin file must be 100% complete with all imports written out explicitly, every XML file must be complete and valid, the AndroidManifest must declare every Activity and Service and Receiver and Permission used in the project, widget provider XML files must be correct and must match the AppWidgetProvider class names exactly, Room database entities and DAOs and the database class must all be present and fully consistent with each other, no file can reference any class or resource or method that is not defined somewhere else in the project, the build.yml must work without any secrets or signing configs or local SDK paths, gradle-wrapper.properties must point to a real stable Gradle 8.x version, no deprecated APIs must be used that would cause build failures on SDK 34, and the app must request BIND_APPWIDGET and handle the widget pinning permission correctly. After generating all files you must verify cross-file consistency by checking that every class referenced in one file is defined in another file, every resource name used in XML exists in the res folder, and the AndroidManifest declares every component used anywhere in the project, and you must fix any inconsistencies found before considering the task complete. All files must be delivered as one complete project ready to be pushed to GitHub via git, and after all files are written provide a plain-English section with numbered steps explaining exactly how to initialize a git repo in the project folder, push it to GitHub, enable GitHub Actions, and download the final APK, written for someone with absolutely zero coding experience.
